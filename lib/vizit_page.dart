import 'package:flutter/material.dart';

class VizitPage extends StatelessWidget {
  const VizitPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.blue,
        title: Text('Vizit Page'),
      ),
    );
  }
}
